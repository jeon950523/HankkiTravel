# Leave empty in local development to use the Vite proxy at http://localhost:8300.
# Set this in Vercel Production to https://api.hankki.kro.kr.
VITE_API_BASE_URL=

# Browser-exposed map SDK key only; never add server secrets with a VITE_ prefix.
VITE_KAKAO_MAP_JS_KEY=
