apiVersion: v1
kind: Service
metadata:
  name: hankki-travel-api
  namespace: hankki
  labels:
    app.kubernetes.io/name: hankki-travel-api
    app.kubernetes.io/part-of: hankki-travel
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hankki-travel-api
  ports:
    - name: http
      port: 8300
      targetPort: 8300
      protocol: TCP
